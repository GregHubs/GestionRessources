<ul>
    <li>Date de la demande : <span class="blue-violet">{{ day.startDate.date|date('d-m-y') }}</span></li>
    <li>Créateur de la demande : <span class="blue-violet">{{ day.user.firstname }}</span></li>
    <li>Date effective de l'absence / présence :  <span class="blue-violet">{{ day.startDate.date|date('d-m-y') }}</span></li>
    {% if day.hours != 0 %}{<li>Durée effective : <span class="blue-violet">{{  day.hours }}</span></li>{% endif %}
    <li>Type de la demande : <span class="blue-violet">{{ day.absenceType.name }}</span></li>
    <li>Description : <span class="blue-violet">{{ day.description }}</span></li>
    <li>Date de la validation : <span class="blue-violet">{{ day.validationDate.date|date('d-m-y') }}</span></li>
</ul>